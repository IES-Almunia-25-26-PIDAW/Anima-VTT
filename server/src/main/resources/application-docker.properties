# Spring profile active when running inside Docker (SPRING_PROFILES_ACTIVE=docker).
# Overrides application.properties paths to match the container filesystem.

spring.datasource.url=jdbc:sqlite:/app/data/database.db
spring.web.resources.static-locations=file:/app/static/
app.upload.dir=/app/uploads
